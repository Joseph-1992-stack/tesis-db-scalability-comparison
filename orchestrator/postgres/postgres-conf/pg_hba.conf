# TYPE  DATABASE        USER            ADDRESS                 METHOD
# Local dentro del contenedor
local   all             all                                     trust

# Loopback
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# Solo red Docker del experimento
host    all             all             172.20.0.0/16           trust

# (Opcional) si necesitara que mi HOST se conecte
# host    all             all             172.20.0.1/32         trust
